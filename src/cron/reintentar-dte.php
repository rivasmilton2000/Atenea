<?php
declare(strict_types=1);require_once dirname(__DIR__,2).'/includes/auth.php';require_once dirname(__DIR__,2).'/includes/dte.php';
if(PHP_SAPI!=='cli'){http_response_code(404);exit;}$pdo=obtenerConexion();$q=$pdo->query("SELECT p.id FROM pedidos p LEFT JOIN dte_documentos d ON d.pedido_id=p.id WHERE p.estado='pagado' AND p.payment_status='paid' AND d.id IS NULL ORDER BY p.paid_at LIMIT 20");$ok=0;$fallos=0;foreach($q->fetchAll(PDO::FETCH_COLUMN)as$id){try{generarDtePedido((int)$id);enviarAvisoDteDisponibleAtenea((int)$id);$ok++;}catch(Throwable){$fallos++;}}echo json_encode(['procesados'=>$ok,'fallos'=>$fallos],JSON_UNESCAPED_UNICODE).PHP_EOL;
